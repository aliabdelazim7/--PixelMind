# 🔐 دليل الإعداد الأمني بعد التحديث (اقرأه قبل النشر)

بعد إصلاحات الأمان، لم تعد أي توكنات (أسرار) مكتوبة داخل الكود. صارت تُقرأ من
**Script Properties** في مشروع Google Apps Script. لازم تنفّذ الخطوات دي مرة واحدة
عشان التطبيق يشتغل صح.

> ⚠️ **مهم جداً:** التوكنات القديمة كانت مكشوفة في الكود وفي تاريخ Git.
> اعتبرها **مسرّبة** ولازم **تدوّرها (تعمل Regenerate) فوراً** — مش بس تنقلها.

---

## 1) خُد نسخة احتياطية من الشيت (قبل أي حاجة)
افتح Google Sheet الخاص بالعملاء → **File → Make a copy**. ده يحميك من أي فقدان بيانات.

## 2) دوّر التوكنات المسرّبة
- **Telegram:** كلّم `@BotFather` → `/revoke` أو `/token` لإصدار توكن جديد للبوت.
- **Meta:** من Meta Events Manager / Business Settings → أنشئ **Access Token جديد** لـ Conversions API، وألغِ القديم.

## 3) اضبط Script Properties في مشروع الـ **Leads** (code.gs)
Apps Script Editor → ⚙️ **Project Settings** → **Script Properties** → أضِف:

| Property | القيمة |
|---|---|
| `API_SECRET` | كلمة سر قوية من اختيارك (مثلاً 32 حرف عشوائي) |
| `SHEET_NAME` | اسم تبويب العملاء (اختياري — الافتراضي أول تبويب) |
| `TELEGRAM_BOT_TOKEN` | توكن البوت **الجديد** |
| `TELEGRAM_CHAT_IDS` | المعرفات مفصولة بفواصل، مثال: `7416290524,5507184715` |
| `META_PIXEL_ID` | معرف البكسل (مثال: `1710050566782840`) |
| `META_ACCESS_TOKEN` | توكن Meta CAPI **الجديد** |

## 4) اضبط نفس كلمة السر في الواجهة
في `index.html` غيّر السطر:
```js
const API_SECRET = "CHANGE_ME_TO_MATCH_SCRIPT_PROPERTY";
```
لنفس قيمة `API_SECRET` اللي حطّيتها في Script Properties.

> ملاحظة صريحة: كلمة السر دي بتظهر في كود الواجهة (JS)، فهي **إجراء وقائي مؤقت**
> يمنع أي حد يعرف رابط الـ `/exec` لوحده من مسح البيانات، لكنها **ليست** أمان
> كامل. الحل النهائي هو تسجيل دخول حقيقي (Auth) — انظر خارطة الطريق بالأسفل.

## 5) أعد النشر
Apps Script → **Deploy → Manage deployments → Edit → New version → Deploy**.
بعدها انشر `index.html` على Vercel كالمعتاد.

## 6) اختبر
- أضف عميل تجريبي → لازم يظهر في الشيت ويوصل إشعار تلجرام.
- غيّر حالة عميل لـ "تم البيع" → لازم يوصل حدث **Purchase** لميتا فقط (مش على كل ليد).
- جرّب تحديد أكثر من عميل → لازم يظهر الشريط العائم ويشتغل الحذف/تغيير الحالة الجماعي.

---

## ما الذي تم إصلاحه في هذا التحديث
- ✅ نقل كل التوكنات للسيرفر (Script Properties) وإزالتها من الواجهة وتاريخ Git.
- ✅ حماية عمليات الكتابة بكلمة سر مشتركة (خاصة الحذف الجماعي).
- ✅ أقفال (LockService) على كل عمليات الكتابة لمنع تكرار/حذف الصفوف الخطأ.
- ✅ منع حقن الصيغ في جداول جوجل (Formula Injection).
- ✅ استهداف تبويب ثابت بدل `getActiveSheet()` العشوائي.
- ✅ **إصلاح حدث ميتا:** بقى يبعت الحدث الصحيح (Lead / Schedule / Purchase) بدل
  إرسال "Purchase" على كل ليد — كان بيفسد استهداف الإعلانات.
- ✅ تفعيل نظام العمليات الجماعية (كان معطّلاً بالكامل) مع حفظ فعلي في الشيت.
- ✅ إصلاح سنة الموعد + تعارض فلتر "اليوم" مع القائمة المنسدلة.
- ✅ تحصين موديول المنتجات (قفل + منع القيم السالبة + منع حقن الصيغ).

## خارطة طريق مقترحة (لاحقاً)
1. **Auth حقيقي** (تسجيل دخول) بدل كلمة السر المشتركة.
2. الانتقال من Google Sheet لقاعدة بيانات حقيقية عند تجاوز بضعة آلاف عميل.
3. فصل `index.html` لمكوّنات + خطوة بناء (حالياً ملف واحد ~3500 سطر).
